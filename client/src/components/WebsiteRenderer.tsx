import { useState } from "react";
import { Globe, MapPin, MessageCircle, Phone } from "lucide-react";
import { trpc } from "@/lib/trpc";

type WebsiteData = { page: { id: number; slug: string }; business: { id: number; name: string; phone?: string | null; whatsapp?: string | null; website?: string | null; address: string; shortDescription?: string | null; aboutDescription?: string | null }; sections: Array<{ id: number; sectionType: string; enabled?: boolean }>; services: Array<{ id: number; name: string; description?: string | null }>; images: Array<{ id: number; url: string; alt?: string | null; imageType: string }>; reviews: Array<{ id: number; content: string | null }>; category?: string | null; city?: string | null };

export default function WebsiteRenderer({ data, preview = false, onTrack }: { data: WebsiteData; preview?: boolean; onTrack?: (eventType: "page_view" | "cta_click" | "lead_start" | "lead_submit" | "call_click" | "whatsapp_click") => void }) {
  const [lead, setLead] = useState({ name: "", email: "", phone: "", message: "", consentGiven: false });
  const createLead = trpc.ai.createLead.useMutation();
  const cover = data.images.find(image => image.imageType === "cover") ?? data.images[0];
  const submit = () => { if (!lead.name.trim() || !lead.consentGiven || preview) return; createLead.mutate({ businessId: data.business.id, name: lead.name.trim(), email: lead.email.trim() || undefined, phone: lead.phone.trim() || undefined, message: lead.message.trim() || undefined, page: data.page.slug, consentGiven: true }, { onSuccess: () => { onTrack?.("lead_submit"); setLead({ name: "", email: "", phone: "", message: "", consentGiven: false }); } }); };
  return <main className="min-h-full bg-slate-50 text-slate-900"><header className="relative overflow-hidden bg-[#102a6b] text-white"><div className="mx-auto max-w-6xl px-5 py-16 sm:px-8">{cover && <img src={cover.url} alt={cover.alt || `${data.business.name} cover`} className="absolute inset-0 size-full object-cover opacity-25" />}<div className="relative max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-blue-200">{data.category ?? "Local business"} · {data.city ?? ""}</p><h1 className="mt-4 text-4xl font-semibold tracking-[-.04em] sm:text-6xl">{data.business.name}</h1><p className="mt-5 max-w-2xl text-base leading-7 text-blue-100">{data.business.shortDescription || "A local business on Just Finds."}</p><div className="mt-7 flex flex-wrap gap-3">{data.business.phone && <a href={`tel:${data.business.phone}`} onClick={() => onTrack?.("call_click")} className="inline-flex items-center gap-2 rounded-xl bg-white px-4 py-3 text-sm font-semibold text-[#102a6b]"><Phone className="size-4" />Call business</a>}{data.business.whatsapp && <a href={`https://wa.me/${data.business.whatsapp.replace(/\D/g, "")}`} onClick={() => onTrack?.("whatsapp_click")} className="inline-flex items-center gap-2 rounded-xl border border-white/30 px-4 py-3 text-sm font-semibold text-white"><MessageCircle className="size-4" />WhatsApp</a>}</div></div></div></header><div className="mx-auto max-w-6xl space-y-8 px-5 py-8 sm:px-8 sm:py-12">{data.sections.filter(section => section.enabled !== false).map(section => <section key={section.id} id={section.sectionType} className="rounded-3xl bg-white p-6 shadow-sm sm:p-8">{section.sectionType === "hero" ? <><p className="text-sm font-semibold text-blue-700">Official business information</p><h2 className="mt-2 text-2xl font-semibold">Welcome to {data.business.name}</h2></> : section.sectionType === "about" ? <><h2 className="text-2xl font-semibold">About</h2><p className="mt-3 max-w-3xl leading-7 text-slate-600">{data.business.aboutDescription || data.business.shortDescription || "Business information is being prepared."}</p></> : ["services", "menu", "rooms", "doctors", "facilities", "offers"].includes(section.sectionType) ? <><h2 className="text-2xl font-semibold capitalize">{section.sectionType}</h2>{data.services.length ? <div className="mt-5 grid gap-3 sm:grid-cols-2">{data.services.map(service => <div key={service.id} className="rounded-2xl border border-slate-200 p-4"><h3 className="font-semibold">{service.name}</h3>{service.description && <p className="mt-2 text-sm leading-6 text-slate-600">{service.description}</p>}</div>)}</div> : <p className="mt-3 text-slate-500">No current offerings have been added.</p>}</> : section.sectionType === "gallery" ? <><h2 className="text-2xl font-semibold">Gallery</h2>{data.images.length ? <div className="mt-5 grid gap-3 sm:grid-cols-3">{data.images.map(image => <img key={image.id} src={image.url} alt={image.alt || `${data.business.name} photo`} className="aspect-[4/3] w-full rounded-2xl object-cover" />)}</div> : <p className="mt-3 text-slate-500">No photos have been added.</p>}</> : section.sectionType === "reviews" ? <><h2 className="text-2xl font-semibold">Reviews</h2>{data.reviews.length ? <div className="mt-5 grid gap-3 sm:grid-cols-2">{data.reviews.map(review => <article key={review.id} className="rounded-2xl border border-slate-200 p-4"><p className="text-sm leading-6 text-slate-600">{review.content || "Published review content is unavailable."}</p></article>)}</div> : <p className="mt-3 text-slate-500">No published reviews are available.</p>}</> : section.sectionType === "contact" || section.sectionType === "map" || section.sectionType === "cta" ? <><h2 className="text-2xl font-semibold">Contact</h2><div className="mt-4 grid gap-3 text-sm text-slate-600"><p className="flex items-start gap-2"><MapPin className="mt-0.5 size-4 text-blue-700" />{data.business.address}</p>{data.business.website && <a href={data.business.website} target="_blank" rel="noreferrer" className="flex items-center gap-2"><Globe className="size-4 text-blue-700" />Official website</a>}</div>{preview ? <p className="mt-5 rounded-xl bg-blue-50 p-3 text-sm text-blue-900">Lead form preview is disabled until this website is public.</p> : <form className="mt-6 grid gap-3" onFocus={() => onTrack?.("lead_start")} onSubmit={event => { event.preventDefault(); submit(); }}><h3 className="font-semibold text-slate-900">Send an enquiry</h3><input required className="field" placeholder="Your name" value={lead.name} onChange={event => setLead({ ...lead, name: event.target.value })} /><input className="field" type="email" placeholder="Email" value={lead.email} onChange={event => setLead({ ...lead, email: event.target.value })} /><textarea className="field min-h-24" placeholder="How can the business help?" value={lead.message} onChange={event => setLead({ ...lead, message: event.target.value })} /><label className="flex items-start gap-2 text-xs text-slate-500"><input type="checkbox" checked={lead.consentGiven} onChange={event => setLead({ ...lead, consentGiven: event.target.checked })} />I agree to share these details with this business for responding to my enquiry.</label><button disabled={!lead.consentGiven || createLead.isPending} className="rounded-xl bg-[#173d9c] px-4 py-3 text-sm font-semibold text-white disabled:opacity-50">{createLead.isPending ? "Sending…" : "Send enquiry"}</button>{createLead.isSuccess && <p className="text-sm text-emerald-700">Your enquiry was sent.</p>}</form>}</> : <><h2 className="text-2xl font-semibold capitalize">{section.sectionType}</h2><p className="mt-3 text-slate-600">Current business information is available from the listing owner.</p></>}</section>)}<footer className="pb-8 text-center text-xs text-slate-500">Published on Just Finds · Business-provided information</footer></div></main>;
}
