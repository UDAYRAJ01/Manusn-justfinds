import { PageFrame } from "@/components/PageFrame";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { ArrowRight, BriefcaseBusiness, Building2, HeartPulse, Home, Landmark, Search, Sparkles, UtensilsCrossed } from "lucide-react";
import { Link } from "wouter";

const categoryIcons: Record<string, React.ElementType> = { healthcare: HeartPulse, "food-dining": UtensilsCrossed, education: Landmark, "home-services": Home, "professional-services": Building2, jobs: BriefcaseBusiness };
const categoryColors = ["bg-[#e7efff] text-[#1f51c8]", "bg-[#f9ece6] text-[#c65d3e]", "bg-[#eaf6ef] text-[#23825a]", "bg-[#f4edfc] text-[#7c43b2]", "bg-[#fff5da] text-[#9c6b1e]", "bg-[#e8f5f7] text-[#147c8c]"];

export default function Categories() {
  const { data, isLoading } = trpc.discovery.categories.useQuery();
  return <PageFrame><section className="border-b border-slate-200 bg-[radial-gradient(circle_at_80%_5%,#e8efff_0,transparent_27%),#fbfcff] py-14 sm:py-20"><div className="container"><span className="inline-flex items-center gap-2 rounded-full border border-blue-100 bg-white px-3 py-1.5 text-xs font-semibold text-[#1f51c8]"><Sparkles className="size-3.5" />Browse with context</span><h1 className="mt-5 max-w-3xl text-4xl font-semibold tracking-[-.06em] text-slate-950 sm:text-6xl">Local categories that know what matters.</h1><p className="mt-5 max-w-2xl text-base leading-7 text-slate-600">Each Just Finds category can carry its own facts and filters — from cuisine and menus to clinical departments, fees, qualifications, availability, and more.</p><Link href="/search"><Button className="mt-7 rounded-xl bg-[#173d9c]">Search all local businesses <Search className="ml-2 size-4" /></Button></Link></div></section><section className="container py-12 sm:py-16"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="eyebrow">Choose a path</p><h2 className="section-title">Explore by need</h2></div><p className="max-w-sm text-sm leading-6 text-slate-500">Admin-defined schemas keep information relevant instead of forcing every local business into the same generic profile.</p></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{isLoading ? [...Array(6)].map((_, i) => <div key={i} className="h-44 animate-pulse rounded-[24px] bg-slate-100" />) : data?.map((category, index) => { const Icon = categoryIcons[category.slug] ?? Building2; return <Link href={`/search?query=${encodeURIComponent(category.name)}`} key={category.id} className="group rounded-[24px] border border-slate-200 bg-white p-5 shadow-[0_8px_22px_rgba(15,23,42,.03)] transition hover:-translate-y-0.5 hover:border-blue-200 hover:shadow-[0_16px_28px_rgba(31,81,200,.10)]"><span className={`grid size-11 place-items-center rounded-2xl ${categoryColors[index % categoryColors.length]}`}><Icon className="size-5" /></span><h3 className="mt-6 text-lg font-semibold tracking-[-.03em] text-slate-900">{category.name}</h3><p className="mt-2 min-h-10 text-xs leading-5 text-slate-500">{category.description ?? "Find local options with decision-ready information and direct contact paths."}</p><span className="mt-5 inline-flex items-center gap-1.5 text-xs font-semibold text-[#1f51c8]">Explore {category.name} <ArrowRight className="size-3.5 transition-transform group-hover:translate-x-1" /></span></Link>; })}</div></section><section className="container pb-14"><div className="rounded-[28px] bg-[#102a6b] px-6 py-8 text-white sm:px-10"><div className="max-w-2xl"><p className="eyebrow !text-blue-200">Built for useful local decisions</p><h2 className="mt-3 text-2xl font-semibold tracking-[-.045em] sm:text-3xl">Not every search deserves the same checklist.</h2><p className="mt-3 text-sm leading-6 text-blue-100/75">Restaurant profiles can show cuisine and menu data. Healthcare profiles can use departments, specialties, and appointment support. Just Finds keeps category design flexible under moderation.</p></div></div></section></PageFrame>;
}
