import { WorkspaceShell } from "@/components/WorkspaceShell";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { BarChart3, Bot, Building2, ClipboardList, FileText, Globe2, Headphones, LayoutDashboard, MessageSquareMore, PanelTop, PhoneCall, Plus, Settings2, Sparkles, UsersRound } from "lucide-react";
import { Link, useLocation } from "wouter";
import { useState } from "react";

const ownerNav = [{ href: "/owner", label: "Overview", icon: LayoutDashboard }, { href: "/owner/profile", label: "Business profiles", icon: Building2 }, { href: "/owner/leads", label: "Leads", icon: UsersRound }, { href: "/owner/content", label: "AI content", icon: Sparkles }, { href: "/owner/jobs", label: "Jobs", icon: ClipboardList }, { href: "/owner/settings", label: "Settings", icon: Settings2 }];

export default function OwnerWorkspace() {
  const { data, isLoading } = trpc.workspace.ownerOverview.useQuery();
  const [location] = useLocation();
  if (location !== "/owner") return <WorkspaceShell title="Business workspace" subtitle="Manage your Just Finds presence" items={ownerNav}><OwnerToolPanel path={location} businesses={data?.businesses ?? []} /></WorkspaceShell>;
  return <WorkspaceShell title="Business workspace" subtitle="Manage your Just Finds presence" items={ownerNav}><div className="mx-auto max-w-6xl"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="eyebrow">Owner control center</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.05em] text-slate-950">Your local presence, in one view.</h1><p className="mt-3 max-w-xl text-sm leading-6 text-slate-500">Each business is isolated to its owner and carries its own category schema, content, leads, and permissions.</p></div><Button className="rounded-xl bg-[#173d9c]"><Plus className="mr-2 size-4" />Add business</Button></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat label="Managed profiles" value={isLoading ? "…" : String(data?.businesses.length ?? 0)} helper="Strictly yours" icon={Building2} /><Stat label="Private leads" value="0" helper="No lead data exposed" icon={UsersRound} /><Stat label="Content readiness" value="—" helper="Approval-led publishing" icon={Sparkles} /><Stat label="Job drafts" value="0" helper="Awaiting submission" icon={ClipboardList} /></div><div className="mt-8 grid gap-5 lg:grid-cols-[1.15fr_.85fr]"><section className="rounded-[24px] border border-slate-200 bg-white p-5 shadow-[0_8px_24px_rgba(15,23,42,.035)]"><div className="flex items-center justify-between"><div><h2 className="font-semibold tracking-[-.02em]">Business profiles</h2><p className="mt-1 text-xs text-slate-500">Approved profile data with dynamic category fields.</p></div><Link href="/owner/profile" className="text-xs font-semibold text-[#1f51c8]">Manage profiles</Link></div><div className="mt-5 grid gap-3">{data?.businesses.length ? data.businesses.map(business => <div key={business.id} className="flex items-center justify-between rounded-2xl bg-slate-50 px-4 py-3"><div><p className="text-sm font-semibold text-slate-800">{business.name}</p><p className="mt-1 text-xs text-slate-500">Status: {business.status.replace(/_/g, " ")}</p></div><Button variant="outline" className="h-8 rounded-lg bg-white text-xs">Edit</Button></div>) : <div className="rounded-2xl border border-dashed border-slate-300 p-7 text-center"><Building2 className="mx-auto size-7 text-slate-400" /><p className="mt-3 text-sm font-semibold text-slate-700">No business profile yet</p><p className="mt-1 text-xs leading-5 text-slate-500">Create a business and the relevant category-specific fields will be selected by the administrator’s schema.</p><Button className="mt-4 h-9 rounded-lg text-xs"><Plus className="mr-1.5 size-3.5" />Create profile</Button></div>}</div></section><section className="rounded-[24px] bg-[#102a6b] p-5 text-white"><span className="grid size-10 place-items-center rounded-xl bg-white/10"><Bot className="size-5" /></span><h2 className="mt-5 text-xl font-semibold tracking-[-.035em]">AI content stays in your control.</h2><p className="mt-3 text-sm leading-6 text-blue-100/75">Generate drafts for SEO, FAQs, landing pages, or voice scripts. Review and approve each result before it reaches your profile.</p><div className="mt-5 grid gap-2"><ToolRow icon={FileText} text="SEO title & meta drafts" /><ToolRow icon={MessageSquareMore} text="Business-only chatbot knowledge" /><ToolRow icon={Headphones} text="Voice introduction script" /><ToolRow icon={PanelTop} text="Landing page configuration" /></div><Link href="/owner/content"><Button className="mt-6 w-full rounded-xl bg-white text-[#102a6b] hover:bg-blue-50">Open AI content studio</Button></Link></section></div><section className="mt-5 grid gap-5 lg:grid-cols-3"><FeatureCard icon={Globe2} title="Custom domains" text="Connect a domain, publish DNS records, and track verification. Domain issuance is kept separate from listing approval." /><FeatureCard icon={PhoneCall} title="Lead inbox" text="Messages and enquiry data remain visible only to the business owner and authorised admins." /><FeatureCard icon={BarChart3} title="Profile intelligence" text="Track discovery and lead signals once public listing analytics are connected." /></section></div></WorkspaceShell>;
}
function Stat({ label, value, helper, icon: Icon }: { label: string; value: string; helper: string; icon: React.ElementType }) { return <div className="rounded-[20px] border border-slate-200 bg-white p-4 shadow-[0_6px_18px_rgba(15,23,42,.025)]"><div className="flex items-start justify-between"><p className="text-xs font-semibold text-slate-500">{label}</p><Icon className="size-4 text-[#1f51c8]" /></div><p className="mt-4 text-2xl font-semibold tracking-[-.04em] text-slate-900">{value}</p><p className="mt-1 text-[11px] text-slate-400">{helper}</p></div>; }
function ToolRow({ icon: Icon, text }: { icon: React.ElementType; text: string }) { return <div className="flex items-center gap-2 rounded-xl bg-white/7 px-3 py-2.5 text-xs text-blue-50"><Icon className="size-3.5 text-blue-200" />{text}</div>; }
function FeatureCard({ icon: Icon, title, text }: { icon: React.ElementType; title: string; text: string }) { return <div className="rounded-[22px] border border-slate-200 bg-white p-5"><span className="grid size-9 place-items-center rounded-xl bg-slate-100 text-slate-700"><Icon className="size-4" /></span><h3 className="mt-4 text-sm font-semibold text-slate-800">{title}</h3><p className="mt-2 text-xs leading-5 text-slate-500">{text}</p></div>; }
function OwnerToolPanel({ path, businesses }: { path: string; businesses: Array<{ id: number; name: string; status: string }> }) {
  const [draft, setDraft] = useState("");
  const [saved, setSaved] = useState(false);
  const content: Record<string, { eyebrow: string; title: string; detail: string; icon: React.ElementType }> = {
    "/owner/profile": { eyebrow: "Profile editor", title: "Build a business profile people can trust.", detail: "Choose the business category first. The form then shows only its administrator-defined fields, ensuring a clinic, restaurant, or educator does not share the same generic checklist.", icon: Building2 },
    "/owner/leads": { eyebrow: "Private lead inbox", title: "Enquiries, kept with the right business.", detail: "Lead data is scoped to your business profile. Assign, annotate, and close a lead without exposing customer information to unrelated businesses.", icon: UsersRound },
    "/owner/content": { eyebrow: "AI content studio", title: "Create drafts. Keep final say.", detail: "Generate approved-data-only drafts for local SEO, FAQs, landing pages, business chatbot context, and an FM-style voice introduction script.", icon: Sparkles },
    "/owner/jobs": { eyebrow: "Employer tools", title: "Publish local roles with a clear approval path.", detail: "Create a draft, receive candidate applications privately, and send your role to Just Finds for moderation before it appears publicly.", icon: ClipboardList },
    "/owner/settings": { eyebrow: "Profile settings", title: "Control how your business is connected.", detail: "Configure custom landing page details, set a custom domain, and track DNS verification separately from your listing approval status.", icon: Settings2 },
  };
  const current = content[path] ?? content["/owner/profile"];
  const Icon = current.icon;
  return <div className="mx-auto max-w-5xl"><p className="eyebrow">{current.eyebrow}</p><div className="mt-2 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><h1 className="text-3xl font-semibold tracking-[-.05em] text-slate-950">{current.title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">{current.detail}</p></div><span className="grid size-12 place-items-center rounded-2xl bg-[#eaf0ff] text-[#1f51c8]"><Icon className="size-6" /></span></div>{path === "/owner/profile" && <section className="mt-8 rounded-[24px] border border-slate-200 bg-white p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Your business profiles</h2><p className="mt-1 text-xs text-slate-500">Profiles visible here are owned by your account only.</p></div><Button className="rounded-xl"><Plus className="mr-2 size-4" />Add profile</Button></div><div className="mt-5 grid gap-3">{businesses.length ? businesses.map(business => <div className="rounded-2xl bg-slate-50 p-4" key={business.id}><div className="flex items-center justify-between"><div><p className="text-sm font-semibold text-slate-800">{business.name}</p><p className="mt-1 text-xs text-slate-500">{business.status.replace(/_/g, " ")} · Dynamic fields follow its category schema</p></div><Button variant="outline" className="h-8 rounded-lg bg-white text-xs">Edit fields</Button></div></div>) : <Empty icon={Building2} label="Start with a category and business name to open your dynamic profile form." />}</div></section>}{path === "/owner/leads" && <section className="mt-8 grid gap-4 lg:grid-cols-[1fr_.8fr]"><div className="rounded-[24px] border border-slate-200 bg-white p-5"><h2 className="font-semibold">Lead inbox</h2><Empty icon={PhoneCall} label="No business enquiries yet. Public enquiry forms will route to the matching business owner here." /></div><div className="rounded-[24px] bg-[#f4efe9] p-5"><h2 className="font-semibold">Lead handling rules</h2><ul className="mt-4 grid gap-3 text-xs leading-5 text-slate-600"><li>• Keep enquiries associated with one business profile.</li><li>• Never share lead details across business owners.</li><li>• Record follow-up consent and disposition.</li></ul></div></section>}{path === "/owner/content" && <section className="mt-8 grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-[24px] border border-slate-200 bg-white p-5"><h2 className="font-semibold">Generate a supervised draft</h2><p className="mt-1 text-xs text-slate-500">This workspace will only accept approved profile facts as AI context.</p><select className="mt-5 h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm"><option>Local SEO title & meta description</option><option>Landing page rewrite</option><option>FAQ set from approved facts</option><option>FM-style voice introduction script</option><option>AI chatbot knowledge refresh</option></select><textarea value={draft} onChange={event => setDraft(event.target.value)} className="mt-3 min-h-28 w-full rounded-xl border border-slate-200 p-3 text-sm" placeholder="Optional guidance for the draft, such as key services or tone." /><Button onClick={() => setSaved(true)} className="mt-3 rounded-xl"><Sparkles className="mr-2 size-4" />Prepare controlled draft</Button>{saved && <p className="mt-3 rounded-xl bg-emerald-50 p-3 text-xs text-emerald-700">Draft request staged for approved-data review. No public content changes are made automatically.</p>}</div><div className="rounded-[24px] bg-[#102a6b] p-5 text-white"><Headphones className="size-6 text-blue-200" /><h2 className="mt-5 text-lg font-semibold">Voice introduction</h2><p className="mt-2 text-sm leading-6 text-blue-100/75">Prepare an FM-style introduction script, choose a voice provider, then review generated audio before it is attached to this profile.</p><Button variant="outline" className="mt-5 rounded-xl border-white/25 bg-white/10 text-white hover:bg-white/20">Configure audio provider</Button><p className="mt-3 text-[11px] leading-5 text-blue-100/60">Audio synthesis remains disconnected until an approved provider and credentials are configured.</p></div></section>}{path === "/owner/jobs" && <section className="mt-8 grid gap-5 lg:grid-cols-[1fr_.8fr]"><div className="rounded-[24px] border border-slate-200 bg-white p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Job drafts & applicants</h2><p className="mt-1 text-xs text-slate-500">Draft → submit for approval → published → applications.</p></div><Button className="rounded-xl"><Plus className="mr-2 size-4" />Create job draft</Button></div><Empty icon={ClipboardList} label="No job drafts yet. Create a role and send it for Just Finds approval." /></div><div className="rounded-[24px] bg-slate-900 p-5 text-white"><h2 className="font-semibold">Applicant privacy</h2><p className="mt-3 text-sm leading-6 text-slate-300">Applicant messages are private to the employer. Only published roles can accept new applications, and admins moderate the role rather than applicant data.</p></div></section>}{path === "/owner/settings" && <section className="mt-8 grid gap-5 lg:grid-cols-2"><div className="rounded-[24px] border border-slate-200 bg-white p-5"><Globe2 className="size-6 text-[#1f51c8]" /><h2 className="mt-4 font-semibold">Custom domain</h2><p className="mt-2 text-xs leading-5 text-slate-500">Add a domain, then publish the DNS record supplied by your domain provider. Verification status does not affect business listing approval.</p><input className="mt-4 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm" placeholder="business.example.com" /><Button onClick={() => setSaved(true)} className="mt-3 rounded-xl">Save domain request</Button>{saved && <p className="mt-3 text-xs text-emerald-700">Domain request saved as pending DNS verification.</p>}</div><div className="rounded-[24px] border border-slate-200 bg-white p-5"><PanelTop className="size-6 text-[#d76546]" /><h2 className="mt-4 font-semibold">Landing page controls</h2><p className="mt-2 text-xs leading-5 text-slate-500">Set a call-to-action, brand color, approved description, and featured services for your Just Finds business landing page.</p><Button variant="outline" className="mt-5 rounded-xl bg-white">Open landing page editor</Button></div></section>}</div>;
}
function Empty({ icon: Icon, label }: { icon: React.ElementType; label: string }) { return <div className="mt-5 grid place-items-center rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-8 text-center"><Icon className="size-7 text-slate-400" /><p className="mt-3 max-w-sm text-sm leading-6 text-slate-500">{label}</p></div>; }
